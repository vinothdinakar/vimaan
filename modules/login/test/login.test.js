'use strict';

var assert = require('assert');

describe('Sample', function() {
    it('should list ALL Vod Assets on /blobs GET');
    it('should list a SINGLE Vod Asset on /blob/<id> GET');
    it('should add a SINGLE Vod Asset /blobs POST');
    it('should delete a SINGLE Vod Asset on /blob/<id> DELETE');
});